{
  "name": "@lou/reviewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Review agent that runs a structured code review through an agent runtime and returns a verdict.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src test",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@lou/opencode-runtime": "workspace:*",
    "@lou/state-machine": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
