{
  "name": "@lou/github",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GitHub adapter driving the gh CLI through the shared command runner.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src test",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@lou/command-runner": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
