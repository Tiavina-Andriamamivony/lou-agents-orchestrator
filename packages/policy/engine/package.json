{
  "name": "@add/policy-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Policy engine: rules, risk classification and capability-based permissions for agent actions.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src test",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
