{
  "name": "@lou/orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The orchestration loop: drives the state machine through the full ticket-to-PR workflow.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src test",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@lou/audit": "workspace:*",
    "@lou/git": "workspace:*",
    "@lou/github": "workspace:*",
    "@lou/opencode-runtime": "workspace:*",
    "@lou/reviewer": "workspace:*",
    "@lou/state-machine": "workspace:*",
    "@lou/test-runner": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
